% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{calibrate_tag}
\alias{calibrate_tag}
\title{Calibrate accelerometer axis parameters}
\usage{
calibrate_tag(df = NULL, acc_x = NULL, acc_y = NULL, acc_z = NULL, ...)
}
\arguments{
\item{df}{A data.frame with \code{acc_x}, \code{acc_y}, and/or \code{acc_z} columns.
If \code{NULL}, at least one of \code{acc_x}, \code{acc_y}, or \code{acc_z} must be provided.}

\item{acc_x, acc_y, acc_z}{Numeric vectors of raw calibration values for
each axis. Ignored when \code{df} is provided.}

\item{...}{Additional arguments passed to \code{\link[=stable_calibration_segs]{stable_calibration_segs()}}.}
}
\value{
A list with elements:
\describe{
\item{offset}{Named numeric vector with per-axis zero-g offsets}
\item{slope}{Named numeric vector with per-axis raw-to-g slopes}
}
}
\description{
Computes per-axis offset and slope values from raw calibration data,
returning named vectors that can be passed directly to \code{\link[=eobs_transform]{eobs_transform()}}.
}
\details{
When \code{df} is a data.frame with \code{acc_x}, \code{acc_y}, and/or \code{acc_z} columns,
each column is calibrated as a separate axis. This is convenient for
calibrating from tabular data files. When \code{df} is \code{NULL}, individual axis
vectors are passed via \code{acc_x}, \code{acc_y}, \code{acc_z}.
}
\examples{
\dontrun{
# Calibrate from a data.frame with acc_x/acc_y/acc_z columns
df <- sim_calibration_data(seed = 42)
cal <- calibrate_tag(df)

# Build a tag_config and transform
tc <- as_tag_config(cal, tag_id = "4501", tag_gen = 3)
eobs_transform(acc_example(), tag_config = tc)

# Or pass offset/slope directly
eobs_transform(acc_example(), tag_gen = 3, offset = cal$offset, slope = cal$slope)
}
}
