% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_units.R
\name{set_imu_units}
\alias{set_imu_units}
\title{Set or convert units on IMU burst data}
\usage{
set_imu_units(x, value, ...)
}
\arguments{
\item{x}{An \code{acc}, \code{mag}, or \code{gyro} vector.}

\item{value}{Character specifying the target units (e.g., \code{"m/s^2"}). For
units in terms of gravitational acceleration, use \code{"standard_free_fall"}.}

\item{...}{Unused.}
}
\value{
The input vector with units attached to each burst matrix.
}
\description{
Methods to attach or convert units on the
burst matrices of \code{acc}, \code{mag}, and \code{gyro} vectors. Each method validates
that the target unit is dimensionally compatible with its IMU class:
\itemize{
\item \code{acc}: acceleration units (e.g., \code{"m/s^2"}, \code{"standard_free_fall"})
\item \code{mag}: magnetic flux density units (e.g., \code{"tesla"}, \code{"uT"}, \code{"gauss"})
\item \code{gyro}: angular velocity units (e.g., \code{"rad/s"}, \code{"degree/s"})
}

To calibrate raw accelerometer values rather than simply attaching or
converting units, use \code{\link[=acc_calibrate]{acc_calibrate()}}.
}
\examples{
a <- acc_example()

# Attach units to unitless bursts
set_imu_units(a, "m/s^2")

# Convert between units
a_ms2 <- set_imu_units(a, "m/s^2")
set_imu_units(a_ms2, "standard_free_fall")

# Dimensionally incompatible units error
try(set_imu_units(a, "kg"))
}
\seealso{
\code{\link[=acc_calibrate]{acc_calibrate()}} to calibrate raw acceleration values.
}
