% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{as_tag_config}
\alias{as_tag_config}
\title{Convert calibration output to a tag_config row}
\usage{
as_tag_config(calibration, tag_id, tag_gen, sensitivity = "low")
}
\arguments{
\item{calibration}{A list with \code{offset} and \code{slope} elements as returned
by \code{\link[=calibrate_tag]{calibrate_tag()}}.}

\item{tag_id}{Tag identifier (character or numeric).}

\item{tag_gen}{Tag generation (numeric), passed to \code{\link[=eobs_transform]{eobs_transform()}}.}

\item{sensitivity}{Sensitivity setting, either \code{"low"} (default) or
\code{"high"}.}
}
\value{
A single-row data.frame with columns \code{tag_id}, \code{tag_gen},
\code{sensitivity}, \code{offset_x}, \code{offset_y}, \code{offset_z}, \code{slope_x}, \code{slope_y},
\code{slope_z}.
}
\description{
Converts the output of \code{\link[=calibrate_tag]{calibrate_tag()}} into a single-row data.frame
compatible with the \code{tag_config} argument of \code{\link[=eobs_transform]{eobs_transform()}}. Multiple
tags can be assembled by row-binding the results.
}
\examples{
\dontrun{
# Single-tag workflow
cal <- calibrate_tag(df)
tc <- as_tag_config(cal, tag_id = "4501", tag_gen = 3)

# Multi-tag assembly
tc <- rbind(
  as_tag_config(cal1, tag_id = "4501", tag_gen = 3),
  as_tag_config(cal2, tag_id = "2100", tag_gen = 1)
)
eobs_transform(acc_data, tag_config = tc)
}
}
