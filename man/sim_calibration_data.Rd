% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{sim_calibration_data}
\alias{sim_calibration_data}
\title{Simulate 3-axis accelerometer calibration data}
\usage{
sim_calibration_data(
  n_per_stable = 500,
  noise_sd = 8,
  frq = 28,
  offset = 0,
  scale = 740,
  seed = NULL
)
}
\arguments{
\item{n_per_stable}{Number of samples per stable orientation. Default 500.}

\item{noise_sd}{Sensor noise standard deviation in raw units during stable
periods. Default 8.}

\item{frq}{Sample rate in Hz. Default 28.}

\item{offset}{Per-axis zero-g level in raw units (the reading when the axis
is perpendicular to gravity). Scalar (recycled to all axes) or named
vector with elements \code{X}, \code{Y}, and/or \code{Z}. Default 0.}

\item{scale}{Per-axis difference between the +1g and -1g readings in raw
units. Controls each axis's sensitivity independently. Scalar (recycled)
or named vector. Default 740 (i.e., +1g reads offset + 370, -1g reads
offset - 370).}

\item{seed}{Random seed for reproducibility. Default \code{NULL} (no seed set).}
}
\value{
A data.frame with columns \code{time}, \code{acc_x}, \code{acc_y}, \code{acc_z}, and
\code{true_label}.
}
\description{
Generates synthetic calibration data for all three axes simultaneously,
simulating a sensor rotated through 6 physically realistic orientations
(±X, ±Y, ±Z). At each orientation, exactly one axis reads ±1g while the
other two read ~0g. Output values are in raw 12-bit ADC scale (0--4095).
}
